#Commands to enter into the terminal to access and modify the tasks in the database.
#To set up the terminal.
python manage.py shell
from experimentation.models import Task

#To see the current tasks in the database.
Task.objects.all().values()

#For adding a single task. Make sure to actually fill in all the empty fields.
task = Task(taskName='', taskDescription='', soloTask=, taskLocation='', taskDate='')
task.save()

#For adding multiple tasks at once. If adding more than two, make sure to properly
#increment and add them all to the list
task0 = Task(taskName='', taskDescription='', soloTask=, taskLocation='', taskDate='')
task1 = Task(taskName='', taskDescription='', soloTask=, taskLocation='', taskDate='')
taskList = [task0, task1]
for x in taskList:
    x.save()

#For updating a task in the database. Make sure to fill in the info correctly.
#Replace "?" with the position of your task in the database, aka the id reduced by one. 
task = Task.objects.all()[?]
#Check to make sure you selected the correct task and you don't accidentally alter a different once.
task.taskName
#Replace "taskName" with the field you intend to change if it's a different one and then fill in a value.
task.taskName = 
task.save()

#For deleting a task from the database.
#Filler task that can be added just to test if the deletion works
task = Task(taskName='NoTask', taskDescription='How did this get in here?', soloTask=False, taskLocation='Nowhere', taskDate='2026-11-1')
#Replace "?" with the position of your task in the database, aka the id reduced by one. 
task = Task.objects.all()[?]
#Check to make sure you selected the correct task and you don't accidentally alter a different once.
task.taskName
#actually delete the task
task.delete()